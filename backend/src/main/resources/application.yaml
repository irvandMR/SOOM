spring:
  application:
    name: backend
  datasource:
    url: jdbc:postgresql://localhost:5432/soom_db
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration
  mail:
    host: smtp.gmail.com
    port: 587
    username: irvandirizky41@gmail.com
    password: henimbezncvlyefk  # paste app password disini, tanpa spasi
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

jwt:
  secret: soom-super-secret-key-ganti-ini-sekarang-2026
  expiration: 900000
  refresh-expiration: 604800000
server:
  port: 8081